# Dependencias de desarrollo (lint, type-check, tests).
# Instalar con: pip install -r requirements-dev.txt
# (incluye requirements.txt automáticamente vía `-e .`).

-r requirements.txt

# Tooling
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
