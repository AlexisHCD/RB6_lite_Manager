# Dependencias de runtime de OpenBuds Manager.
# Versiones pinneadas para reproducibilidad. Ver pyproject.toml [project].
#
# PyGObject puede instalarse de dos formas coherentes:
#
# 1. Distribución (recomendado en Ubuntu):
#      sudo apt install python3-gi python3-gi-cairo gir1.2-glib-2.0
#    Crear el venv con --system-site-packages para reutilizar PyGObject del sistema.
#
# 2. PyPI: si pip compila PyGObject, instalar antes:
#      sudo apt install libgirepository-2.0-dev gcc libcairo2-dev pkg-config python3-dev
#    En este caso el venv puede usar o no --system-site-packages.

PySide6>=6.6,<7
PyGObject>=3.46
PyYAML>=6.0
